# eq

This program download all attachements files to a discord message.
Files are downloaded in the `./dl/` directory and are named with an UUID.  
The bot look for his connection token in the environment variable named `tkn`.
